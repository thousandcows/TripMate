<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="CompanyBoard">
  	
 		<insert id="insert">
 			insert into company_board values(com_seq.nextval, 1, #{title}, #{contents}, #{recruit}, 0, #{tour}, #{gender}, #{start_date}, #{end_date}, default, default, 0, 0, 0)
 		</insert>
 		
 		<select id="selectAll" resultType="kh.spring.dto.CompanyBoardDTO">
 			select * from company_board order by seq
 		</select>
  
  </mapper>